
QUnit.module("swlcalc");

/*Test('should initate swlcalc submodules', 8, function() {
    swlcalc.init();
    ok(swlcalc);
    ok(swlcalc.data);
    ok(swlcalc.swap);
    ok(swlcalc.select);
    ok(swlcalc.buttonBar);
    ok(swlcalc.summary);
    ok(swlcalc.export);
    ok(swlcalc.import);
});*/

QUnit.test("should import from hash", function(assert) {
    location.hash = "weapon=3,4,3,19,3,2,2,13,0,2,0&weapon2=3,7,2,29,4,3,1,15,0,8,0&head=3,4,2,24,4,3,2,18,1,1,16&finger=2,2,1,20,5,4,3,15,4,1,2&neck=1,11,3,6,3,2,1,20,4,1,3&wrist=5,2,2,68,3,3,2,6,5,1,8&luck=1,1,1,10,2,2,1,18,3,1,13&waist=5,11,1,2,4,5,3,8,3,3,16&occult=1,7,3,19,5,1,3,2,5,2,10";
    swlcalc.init();

    // Summary
    assert.equal($("#stat-ilvl").html(), "565");
    assert.equal($("#stat-power-rating").html(), "4388");
    assert.equal($("#stat-weapon-power").html(), "1577");
    assert.equal($("#stat-attack-rating").html(), "8710");
    assert.equal($("#stat-heal-rating").html(), "4310");
    assert.equal($("#stat-hitpoints").html(), "7512");
    assert.equal($("#stat-combat-power").html(), "772");
    assert.equal($("#stat-healing-power").html(), "442");
    assert.equal($("#stat-critical-rating").html(), "+1598");
    assert.equal($("#stat-critical-chance").html(), "18.6%");
    assert.equal($("#stat-critical-power").html(), "+1858");
    assert.equal($("#stat-critical-power-percentage").html(), "120.6%");
    assert.equal($("#stat-hit-rating").html(), "+1539");
    assert.equal($("#stat-glance-reduction").html(), "30.3%");
    assert.equal($("#stat-defense-rating").html(), "+1313");
    assert.equal($("#stat-glance-chance").html(), "12.9%");
    assert.equal($("#stat-evade-rating").html(), "+1805");
    assert.equal($("#stat-evade-chance").html(), "12.4%");

    // Equal($('#stat-physical-protection').html(), '+660');
    // equal($('#stat-magical-protection').html(), '+300');

    assert.ok($("#weapon-slot").is(":visible"));
    assert.equal($("#weapon-ilvl").html(), "400");

    //TODO/FEATURE equal($('#weapon-name').html(), ': Blood');
    assert.equal($("#weapon-equipment-id").val(), "4");
    assert.equal($("#weapon-equipment-rarity").val(), "epic");
    assert.equal($("#weapon-equipment-quality").val(), "3");
    assert.equal($("#weapon-equipment-level").val(), "19");
    assert.equal($("#weapon-equipment-stat-rating").html(), "+1577");
    assert.equal($("#weapon-equipment-ilvl").html(), "264");
    assert.equal($("#weapon-glyph-id").val(), "critical-rating");
    assert.equal($("#weapon-glyph-rarity").val(), "epic");
    assert.equal($("#weapon-glyph-quality").val(), "2");
    assert.equal($("#weapon-glyph-level").val(), "13");
    assert.equal($("#weapon-glyph-stat-rating").html(), "+329");
    assert.equal($("#weapon-glyph-ilvl").html(), "136");
    assert.equal($("#weapon-signet-id").val(), "2");
    assert.equal($("#weapon-signet-rarity").val(), undefined);
    assert.equal($("#weapon-signet-level").val(), undefined);
    assert.equal($("#weapon-signet-ilvl").html(), undefined);
    assert.equal($("#weapon-signet-description").html(), 'This weapon deals <span id="weapon-signet-bonus" class="bonus-val attack">347.4</span> additional damage to enemies that are below <span class="bonus-val const">35%</span> health.');

    assert.ok(!$("#weapon2-active").is(":visible"));
    assert.equal($("#weapon2-ilvl").html(), "581");

    //TODO/FEATURE equal($('#weapon2-name').html(), ': Shotgun');
    assert.equal($("#weapon2-equipment-id").val(), "7");
    assert.equal($("#weapon2-equipment-rarity").val(), "epic");
    assert.equal($("#weapon2-equipment-quality").val(), "2");
    assert.equal($("#weapon2-equipment-level").val(), "29");
    assert.equal($("#weapon2-equipment-stat-rating").html(), "+1809");
    assert.equal($("#weapon2-equipment-ilvl").html(), "315");
    assert.equal($("#weapon2-glyph-id").val(), "critical-power");
    assert.equal($("#weapon2-glyph-rarity").val(), "mythic");
    assert.equal($("#weapon2-glyph-quality").val(), "1");
    assert.equal($("#weapon2-glyph-level").val(), "15");
    assert.equal($("#weapon2-glyph-stat-rating").html(), "+491");
    assert.equal($("#weapon2-glyph-ilvl").html(), "266");
    assert.equal($("#weapon2-signet-id").val(), "8");
    assert.equal($("#weapon2-signet-rarity").val(), undefined);
    assert.equal($("#weapon2-signet-level").val(), undefined);
    assert.equal($("#weapon2-signet-ilvl").html(), undefined);
    assert.equal($("#weapon2-signet-description").html(), 'Increases Protection by <span id="weapon2-signet-bonus" class="bonus-val const">300</span> while wielded.');

    assert.equal($("#head-ilvl").html(), "487");
    assert.equal($("#head-equipment-id").val(), "4");
    assert.equal($("#head-equipment-rarity").val(), "epic");
    assert.equal($("#head-equipment-quality").val(), "2");
    assert.equal($("#head-equipment-level").val(), "24");
    assert.equal($("#head-equipment-stat-rating").html(), "+1113");
    assert.equal($("#head-equipment-ilvl").html(), "252");
    assert.equal($("#head-equipment-description").html(), 'Whenever you hit the same enemy <span class="bonus-val const">3</span> times in succession, you deal <span id="head-equipment-bonus1" class="bonus-val attack">290</span> physical damage.');
    assert.equal($("#head-glyph-id").val(), "critical-power");
    assert.equal($("#head-glyph-rarity").val(), "mythic");
    assert.equal($("#head-glyph-quality").val(), "2");
    assert.equal($("#head-glyph-level").val(), "18");
    assert.equal($("#head-glyph-stat-rating").html(), "+580");
    assert.equal($("#head-glyph-ilvl").html(), "226");
    assert.equal($("#head-signet-id").val(), "1");
    assert.equal($("#head-signet-rarity").val(), "standard");
    assert.equal($("#head-signet-level").val(), "16");
    assert.equal($("#head-signet-ilvl").html(), "10");
    assert.equal($("#head-signet-description").html(), 'Increases the damage and healing of Pistols Elite Abilities by <span id="head-signet-bonus" class="bonus-val const">3.4495</span><span class="bonus-val const">%</span>.');

    assert.equal($("#finger-ilvl").html(), "571");
    assert.equal($("#finger-equipment-id").val(), "2");
    assert.equal($("#finger-equipment-rarity").val(), "superior");
    assert.equal($("#finger-equipment-quality").val(), "1");
    assert.equal($("#finger-equipment-level").val(), "20");
    assert.equal($("#finger-equipment-stat-rating").html(), "+314");
    assert.equal($("#finger-equipment-ilvl").html(), "113");
    assert.equal($("#finger-equipment-description").html(), 'Whenever you purge a beneficial effect from an enemy\'s ability, your Protection is increased by <span class="bonus-val const">1530</span> for <span class="bonus-val const">4</span> seconds.');
    assert.equal($("#finger-glyph-id").val(), "evade-rating");
    assert.equal($("#finger-glyph-rarity").val(), "legendary");
    assert.equal($("#finger-glyph-quality").val(), "3");
    assert.equal($("#finger-glyph-level").val(), "15");
    assert.equal($("#finger-glyph-stat-rating").html(), "+1052");
    assert.equal($("#finger-glyph-ilvl").html(), "369");
    assert.equal($("#finger-signet-id").val(), "1");
    assert.equal($("#finger-signet-rarity").val(), "mythic");
    assert.equal($("#finger-signet-level").val(), "2");
    assert.equal($("#finger-signet-ilvl").html(), "89");
    assert.equal($("#finger-signet-description").html(), 'Increases the damage and healing of Pistols Basic Abilities by <span id="finger-signet-bonus" class="bonus-val const">47.9079</span><span class="bonus-val const">%</span>.');

    assert.equal($("#neck-ilvl").html(), "232");
    assert.equal($("#neck-equipment-id").val(), "11");
    assert.equal($("#neck-equipment-rarity").val(), "standard");
    assert.equal($("#neck-equipment-quality").val(), "3");
    assert.equal($("#neck-equipment-level").val(), "6");
    assert.equal($("#neck-equipment-stat-rating").html(), "+70");
    assert.equal($("#neck-equipment-ilvl").html(), "13");
    assert.equal($("#neck-equipment-description").html(), 'This talisman gains more health when leveling up.<br>Whenever you lose health while below <span class="bonus-val const">50%</span> of your maximum health, your damage is increased by <span class="bonus-val const">17%</span> for <span class="bonus-val const">3</span> seconds and your next attack during this time will restore <span class="bonus-val const">4%</span> of your maximum health. This effect can only occur once every <span class="bonus-val const">10</span> seconds.');
    assert.equal($("#neck-glyph-id").val(), "critical-rating");
    assert.equal($("#neck-glyph-rarity").val(), "epic");
    assert.equal($("#neck-glyph-quality").val(), "1");
    assert.equal($("#neck-glyph-level").val(), "20");
    assert.equal($("#neck-glyph-stat-rating").html(), "+339");
    assert.equal($("#neck-glyph-ilvl").html(), "128");
    assert.equal($("#neck-signet-id").val(), "1");
    assert.equal($("#neck-signet-rarity").val(), "mythic");
    assert.equal($("#neck-signet-level").val(), "3");
    assert.equal($("#neck-signet-ilvl").html(), "91");
    assert.equal($("#neck-signet-description").html(), 'Increases the damage and healing of Pistols Power Abilities by <span id="neck-signet-bonus" class="bonus-val const">12.6405</span><span class="bonus-val const">%</span>.');

    assert.equal($("#wrist-ilvl").html(), "1292");
    assert.equal($("#wrist-equipment-id").val(), "2");
    assert.equal($("#wrist-equipment-rarity").val(), "legendary");
    assert.equal($("#wrist-equipment-quality").val(), "2");
    assert.equal($("#wrist-equipment-level").val(), "68");
    assert.equal($("#wrist-equipment-stat-rating").html(), "+1951");
    assert.equal($("#wrist-equipment-ilvl").html(), "1026");
    assert.equal($("#wrist-equipment-description").html(), 'Whenever you heal an ally who does not have any active detrimental effects, you have a <span class="bonus-val const">25%</span> chance to restore <span id="wrist-equipment-bonus1" class="bonus-val heal">221</span> health to that ally.');
    assert.equal($("#wrist-glyph-id").val(), "critical-power");
    assert.equal($("#wrist-glyph-rarity").val(), "epic");
    assert.equal($("#wrist-glyph-quality").val(), "2");
    assert.equal($("#wrist-glyph-level").val(), "6");
    assert.equal($("#wrist-glyph-stat-rating").html(), "+270");
    assert.equal($("#wrist-glyph-ilvl").html(), "94");
    assert.equal($("#wrist-signet-id").val(), "1");
    assert.equal($("#wrist-signet-rarity").val(), "legendary");
    assert.equal($("#wrist-signet-level").val(), "8");
    assert.equal($("#wrist-signet-ilvl").html(), "172");
    assert.equal($("#wrist-signet-description").html(), 'Increases the rate at which your Ultimate Ability recharges on ability activation by <span id="wrist-signet-bonus" class="bonus-val const">36.1004</span><span class="bonus-val const">%</span>.');

    assert.equal($("#luck-ilvl").html(), "136");
    assert.equal($("#luck-equipment-id").val(), "1");
    assert.equal($("#luck-equipment-rarity").val(), "standard");
    assert.equal($("#luck-equipment-quality").val(), "1");
    assert.equal($("#luck-equipment-level").val(), "10");
    assert.equal($("#luck-equipment-stat-rating").html(), "+53");
    assert.equal($("#luck-equipment-ilvl").html(), "22");
    assert.equal($("#luck-equipment-description").html(), "");
    assert.equal($("#luck-glyph-id").val(), "critical-rating");
    assert.equal($("#luck-glyph-rarity").val(), "superior");
    assert.equal($("#luck-glyph-quality").val(), "1");
    assert.equal($("#luck-glyph-level").val(), "18");
    assert.equal($("#luck-glyph-stat-rating").html(), "+174");
    assert.equal($("#luck-glyph-ilvl").html(), "58");
    assert.equal($("#luck-signet-id").val(), "1");
    assert.equal($("#luck-signet-rarity").val(), "epic");
    assert.equal($("#luck-signet-level").val(), "13");
    assert.equal($("#luck-signet-ilvl").html(), "55");
    assert.equal($("#luck-signet-description").html(), 'When you critically heal, you gain a beneficial effect that grants you a <span id="luck-signet-bonus" class="bonus-val heal">17.8511</span><span class="bonus-val heal">%</span> bonus to healing for <span class="bonus-val const">5</span> seconds. This ability can only occur once every <span class="bonus-val const">15</span> seconds.');

    assert.equal($("#waist-ilvl").html(), "846");
    assert.equal($("#waist-equipment-id").val(), "11");
    assert.equal($("#waist-equipment-rarity").val(), "legendary");
    assert.equal($("#waist-equipment-quality").val(), "1");
    assert.equal($("#waist-equipment-level").val(), "2");
    assert.equal($("#waist-equipment-stat-rating").html(), "+775");
    assert.equal($("#waist-equipment-ilvl").html(), "591");
    assert.equal($("#waist-equipment-description").html(), 'Whenever you spend Rage to empower an attack, you gain <span class="bonus-val const">7</span> Rage. Rage must be spent in order to trigger this effect.');
    assert.equal($("#waist-glyph-id").val(), "defense-rating");
    assert.equal($("#waist-glyph-rarity").val(), "mythic");
    assert.equal($("#waist-glyph-quality").val(), "3");
    assert.equal($("#waist-glyph-level").val(), "8");
    assert.equal($("#waist-glyph-stat-rating").html(), "+560");
    assert.equal($("#waist-glyph-ilvl").html(), "196");
    assert.equal($("#waist-signet-id").val(), "3");
    assert.equal($("#waist-signet-rarity").val(), "epic");
    assert.equal($("#waist-signet-level").val(), "16");
    assert.equal($("#waist-signet-ilvl").html(), "59");
    assert.equal($("#waist-signet-description").html(), 'Reduces the cooldown of Gadgets by <span id="waist-signet-bonus" class="bonus-val const">32.7158</span><span class="bonus-val const">%</span>.');

    assert.equal($("#occult-ilvl").html(), "540");
    assert.equal($("#occult-equipment-id").val(), "7");
    assert.equal($("#occult-equipment-rarity").val(), "standard");
    assert.equal($("#occult-equipment-quality").val(), "3");
    assert.equal($("#occult-equipment-level").val(), "19");
    assert.equal($("#occult-equipment-stat-rating").html(), "+112");
    assert.equal($("#occult-equipment-ilvl").html(), "42");
    assert.equal($("#occult-equipment-description").html(), 'Whenever you or any of your group members defeat an enemy, your Protection is increased by <span class="bonus-val const">2045</span> for <span class="bonus-val const">5</span> seconds.');
    assert.equal($("#occult-glyph-id").val(), "hit-rating");
    assert.equal($("#occult-glyph-rarity").val(), "legendary");
    assert.equal($("#occult-glyph-quality").val(), "3");
    assert.equal($("#occult-glyph-level").val(), "2");
    assert.equal($("#occult-glyph-stat-rating").html(), "+783");
    assert.equal($("#occult-glyph-ilvl").html(), "322");
    assert.equal($("#occult-signet-id").val(), "2");
    assert.equal($("#occult-signet-rarity").val(), "legendary");
    assert.equal($("#occult-signet-level").val(), "10");
    assert.equal($("#occult-signet-ilvl").html(), "175");
    assert.equal($("#occult-signet-description").html(), 'Active dodges break movement hindering effects, but also have their cooldown increased by <span id="occult-signet-bonus" class="bonus-val const">56.4684</span><span class="bonus-val const">%</span>.');
});

QUnit.test("should set default values with an empty hash", function(assert) {
    location.hash = "";
    swlcalc.init();

    // Summary
    // --- Primary Stats
    assert.equal($("#stat-ilvl").html(), "0");
    assert.equal($("#stat-power-rating").html(), "0");
    assert.equal($("#stat-weapon-power").html(), "0");
    assert.equal($("#stat-attack-rating").html(), "4322");
    assert.equal($("#stat-heal-rating").html(), "4310");
    assert.equal($("#stat-hitpoints").html(), "7512");
    assert.equal($("#stat-combat-power").html(), "324");
    assert.equal($("#stat-healing-power").html(), "323");

    // --- Offensive Stats
    assert.equal($("#stat-critical-rating").html(), "+756");
    assert.equal($("#stat-critical-chance").html(), "13.3%");
    assert.equal($("#stat-critical-power").html(), "+1008");
    assert.equal($("#stat-critical-power-percentage").html(), "90.6%");
    assert.equal($("#stat-hit-rating").html(), "+756");
    assert.equal($("#stat-glance-reduction").html(), "14.9%");

    // --- Defensive Stats
    assert.equal($("#stat-defense-rating").html(), "+753");
    assert.equal($("#stat-glance-chance").html(), "7.4%");
    assert.equal($("#stat-evade-rating").html(), "+753");
    assert.equal($("#stat-evade-chance").html(), "5.2%");

    // Equal($('#stat-physical-protection').html(), '+660');
    // equal($('#stat-magical-protection').html(), '+300');

    assert.ok($("#weapon-active").is(":visible"));
    assert.equal($("#weapon-ilvl").html(), "0");

    //TODO/FEATURE equal($('#weapon-name').html(), '');
    assert.equal($("#weapon-equipment-id").val(), "none");
    assert.equal($("#weapon-equipment-rarity").val(), "standard");
    assert.equal($("#weapon-equipment-quality").val(), "1");
    assert.equal($("#weapon-equipment-level").val(), "20");
    assert.equal($("#weapon-equipment-stat-rating").html(), "0");
    assert.equal($("#weapon-equipment-ilvl").html(), "0");
    assert.equal($("#weapon-glyph-id").val(), "none");
    assert.equal($("#weapon-glyph-rarity").val(), "standard");
    assert.equal($("#weapon-glyph-quality").val(), "1");
    assert.equal($("#weapon-glyph-level").val(), "20");
    assert.equal($("#weapon-glyph-stat-rating").html(), "0");
    assert.equal($("#weapon-glyph-ilvl").html(), "0");
    assert.equal($("#weapon-signet-id").val(), "none");
    assert.equal($("#weapon-signet-rarity").val(), undefined);
    assert.equal($("#weapon-signet-level").val(), undefined);
    assert.equal($("#weapon-signet-ilvl").html(), undefined);
    assert.equal($("#weapon-signet-description").html(), "");
    assert.equal($("#weapon-signet-bonus").val(), undefined);

    assert.ok(!$("#weapon2-active").is(":visible"));
    assert.equal($("#weapon2-ilvl").html(), "0");

    //TODO/FEATURE equal($('#weapon2-name').html(), '');
    assert.equal($("#weapon2-equipment-id").val(), "none");
    assert.equal($("#weapon2-equipment-rarity").val(), "standard");
    assert.equal($("#weapon2-equipment-quality").val(), "1");
    assert.equal($("#weapon2-equipment-level").val(), "20");
    assert.equal($("#weapon2-equipment-stat-rating").html(), "0");
    assert.equal($("#weapon2-equipment-ilvl").html(), "0");
    assert.equal($("#weapon2-glyph-id").val(), "none");
    assert.equal($("#weapon2-glyph-rarity").val(), "standard");
    assert.equal($("#weapon2-glyph-quality").val(), "1");
    assert.equal($("#weapon2-glyph-level").val(), "20");
    assert.equal($("#weapon2-glyph-stat-rating").html(), "0");
    assert.equal($("#weapon2-glyph-ilvl").html(), "0");
    assert.equal($("#weapon2-signet-id").val(), "none");
    assert.equal($("#weapon2-signet-rarity").val(), undefined);
    assert.equal($("#weapon2-signet-level").val(), undefined);
    assert.equal($("#weapon2-signet-ilvl").html(), undefined);
    assert.equal($("#weapon2-signet-description").html(), "");
    assert.equal($("#weapon2-signet-bonus").val(), undefined);

    assert.equal($("#head-ilvl").html(), "0");
    assert.equal($("#head-equipment-id").val(), "none");
    assert.equal($("#head-equipment-rarity").val(), "standard");
    assert.equal($("#head-equipment-quality").val(), "1");
    assert.equal($("#head-equipment-level").val(), "20");
    assert.equal($("#head-equipment-stat-rating").html(), "0");
    assert.equal($("#head-equipment-ilvl").html(), "0");
    assert.equal($("#head-glyph-id").val(), "none");
    assert.equal($("#head-glyph-rarity").val(), "standard");
    assert.equal($("#head-glyph-quality").val(), "1");
    assert.equal($("#head-glyph-level").val(), "20");
    assert.equal($("#head-glyph-stat-rating").html(), "0");
    assert.equal($("#head-glyph-ilvl").html(), "0");
    assert.equal($("#head-signet-id").val(), "none");
    assert.equal($("#head-signet-rarity").val(), "standard");
    assert.equal($("#head-signet-level").val(), "20");
    assert.equal($("#head-signet-ilvl").html(), "0");
    assert.equal($("#head-signet-description").html(), "");
    assert.equal($("#head-signet-bonus").val(), undefined);

    assert.equal($("#finger-ilvl").html(), "0");
    assert.equal($("#finger-equipment-id").val(), "none");
    assert.equal($("#finger-equipment-rarity").val(), "standard");
    assert.equal($("#finger-equipment-quality").val(), "1");
    assert.equal($("#finger-equipment-level").val(), "20");
    assert.equal($("#finger-equipment-stat-rating").html(), "0");
    assert.equal($("#finger-equipment-ilvl").html(), "0");
    assert.equal($("#finger-glyph-id").val(), "none");
    assert.equal($("#finger-glyph-rarity").val(), "standard");
    assert.equal($("#finger-glyph-quality").val(), "1");
    assert.equal($("#finger-glyph-level").val(), "20");
    assert.equal($("#finger-glyph-stat-rating").html(), "0");
    assert.equal($("#finger-glyph-ilvl").html(), "0");
    assert.equal($("#finger-signet-id").val(), "none");
    assert.equal($("#finger-signet-rarity").val(), "standard");
    assert.equal($("#finger-signet-level").val(), "20");
    assert.equal($("#finger-signet-ilvl").html(), "0");
    assert.equal($("#finger-signet-description").html(), "");
    assert.equal($("#finger-signet-bonus").val(), undefined);

    assert.equal($("#neck-ilvl").html(), "0");
    assert.equal($("#neck-equipment-id").val(), "none");
    assert.equal($("#neck-equipment-rarity").val(), "standard");
    assert.equal($("#neck-equipment-quality").val(), "1");
    assert.equal($("#neck-equipment-level").val(), "20");
    assert.equal($("#neck-equipment-stat-rating").html(), "0");
    assert.equal($("#neck-equipment-ilvl").html(), "0");
    assert.equal($("#neck-glyph-id").val(), "none");
    assert.equal($("#neck-glyph-rarity").val(), "standard");
    assert.equal($("#neck-glyph-quality").val(), "1");
    assert.equal($("#neck-glyph-level").val(), "20");
    assert.equal($("#neck-glyph-stat-rating").html(), "0");
    assert.equal($("#neck-glyph-ilvl").html(), "0");
    assert.equal($("#neck-signet-id").val(), "none");
    assert.equal($("#neck-signet-rarity").val(), "standard");
    assert.equal($("#neck-signet-level").val(), "20");
    assert.equal($("#neck-signet-ilvl").html(), "0");
    assert.equal($("#neck-signet-description").html(), "");
    assert.equal($("#neck-signet-bonus").val(), undefined);

    assert.equal($("#wrist-ilvl").html(), "0");
    assert.equal($("#wrist-equipment-id").val(), "none");
    assert.equal($("#wrist-equipment-rarity").val(), "standard");
    assert.equal($("#wrist-equipment-quality").val(), "1");
    assert.equal($("#wrist-equipment-level").val(), "20");
    assert.equal($("#wrist-equipment-stat-rating").html(), "0");
    assert.equal($("#wrist-equipment-ilvl").html(), "0");
    assert.equal($("#wrist-glyph-id").val(), "none");
    assert.equal($("#wrist-glyph-rarity").val(), "standard");
    assert.equal($("#wrist-glyph-quality").val(), "1");
    assert.equal($("#wrist-glyph-level").val(), "20");
    assert.equal($("#wrist-glyph-stat-rating").html(), "0");
    assert.equal($("#wrist-glyph-ilvl").html(), "0");
    assert.equal($("#wrist-signet-id").val(), "none");
    assert.equal($("#wrist-signet-rarity").val(), "standard");
    assert.equal($("#wrist-signet-level").val(), "20");
    assert.equal($("#wrist-signet-ilvl").html(), "0");
    assert.equal($("#wrist-signet-description").html(), "");
    assert.equal($("#wrist-signet-bonus").val(), undefined);

    assert.equal($("#luck-ilvl").html(), "0");
    assert.equal($("#luck-equipment-id").val(), "none");
    assert.equal($("#luck-equipment-rarity").val(), "standard");
    assert.equal($("#luck-equipment-quality").val(), "1");
    assert.equal($("#luck-equipment-level").val(), "20");
    assert.equal($("#luck-equipment-stat-rating").html(), "0");
    assert.equal($("#luck-equipment-ilvl").html(), "0");
    assert.equal($("#luck-glyph-id").val(), "none");
    assert.equal($("#luck-glyph-rarity").val(), "standard");
    assert.equal($("#luck-glyph-quality").val(), "1");
    assert.equal($("#luck-glyph-level").val(), "20");
    assert.equal($("#luck-glyph-stat-rating").html(), "0");
    assert.equal($("#luck-glyph-ilvl").html(), "0");
    assert.equal($("#luck-signet-id").val(), "none");
    assert.equal($("#luck-signet-rarity").val(), "standard");
    assert.equal($("#luck-signet-level").val(), "20");
    assert.equal($("#luck-signet-ilvl").html(), "0");
    assert.equal($("#luck-signet-description").html(), "");
    assert.equal($("#luck-signet-bonus").val(), undefined);

    assert.equal($("#waist-ilvl").html(), "0");
    assert.equal($("#waist-equipment-id").val(), "none");
    assert.equal($("#waist-equipment-rarity").val(), "standard");
    assert.equal($("#waist-equipment-quality").val(), "1");
    assert.equal($("#waist-equipment-level").val(), "20");
    assert.equal($("#waist-equipment-stat-rating").html(), "0");
    assert.equal($("#waist-equipment-ilvl").html(), "0");
    assert.equal($("#waist-glyph-id").val(), "none");
    assert.equal($("#waist-glyph-rarity").val(), "standard");
    assert.equal($("#waist-glyph-quality").val(), "1");
    assert.equal($("#waist-glyph-level").val(), "20");
    assert.equal($("#waist-glyph-stat-rating").html(), "0");
    assert.equal($("#waist-glyph-ilvl").html(), "0");
    assert.equal($("#waist-signet-id").val(), "none");
    assert.equal($("#waist-signet-rarity").val(), "standard");
    assert.equal($("#waist-signet-level").val(), "20");
    assert.equal($("#waist-signet-ilvl").html(), "0");
    assert.equal($("#waist-signet-description").html(), "");
    assert.equal($("#waist-signet-bonus").val(), undefined);

    assert.equal($("#occult-ilvl").html(), "0");
    assert.equal($("#occult-equipment-id").val(), "none");
    assert.equal($("#occult-equipment-rarity").val(), "standard");
    assert.equal($("#occult-equipment-quality").val(), "1");
    assert.equal($("#occult-equipment-level").val(), "20");
    assert.equal($("#occult-equipment-stat-rating").html(), "0");
    assert.equal($("#occult-equipment-ilvl").html(), "0");
    assert.equal($("#occult-glyph-id").val(), "none");
    assert.equal($("#occult-glyph-rarity").val(), "standard");
    assert.equal($("#occult-glyph-quality").val(), "1");
    assert.equal($("#occult-glyph-level").val(), "20");
    assert.equal($("#occult-glyph-stat-rating").html(), "0");
    assert.equal($("#occult-glyph-ilvl").html(), "0");
    assert.equal($("#occult-signet-id").val(), "none");
    assert.equal($("#occult-signet-rarity").val(), "standard");
    assert.equal($("#occult-signet-level").val(), "20");
    assert.equal($("#occult-signet-ilvl").html(), "0");
    assert.equal($("#occult-signet-description").html(), "");
    assert.equal($("#occult-signet-bonus").val(), undefined);
});
